transformer idBool(a : ! bool) -> res : ! bool {
    a --> res
}

transformer notBool(a : ! bool) -> res : ! bool {
    try {
        a --[ ! such that idBool() ]-> var y : ! bool
        false --> res
    } catch {
        true --> res
    }
}

;

false --> var b : ! bool
b --[ ! such that notBool() ]-> var c : ! bool

