type Token is fungible asset nat
type ether is fungible asset nat
type Message is asset { sender : ! address, value : any ether }
/* transformer transfer(balances : any map ! address => any Token, msg : ! Message, dst : ! address, amount : any nat) -> res : ! bool { */
/*   balances[msg.sender][amount] --> balances[dst] */
/*   true --> res */
/* } */

;

10 --> new ether() --> var val : ether
val --> new Message(0) --> var msg : Message

[] --> var balances : map ! address => any Token
balances[msg.sender][amount] --> balances[dst]
true --> res

