type Token is fungible asset nat
/* transformer transfer(balances : any map ! address => any Token, sender : ! address, dst : ! address, amount : any nat) -> res : ! bool { */
/*   balances[sender][amount] --> balances[dst] */
/*   true --> res */
/* } */

;

[] --> var balances : map ! address => any Token
// TODO: Need to have address literals (i.e., hex literals) in the compiler
balances[0][amount] --> balances[1]
true --> res

