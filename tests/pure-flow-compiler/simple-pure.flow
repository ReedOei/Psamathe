type Token is unique consumable asset nat

type Bid is {
    sender : one address,
    amount : any nat
}

transformer id(b : any bool) -> res : any bool {
    b --> res
}

transformer transfer(balances : map any nat => nat, src : nat, amount : nat, dst : nat) {
    balances[src].value --[ amount ]-> balances[dst].value
}

;

1 --> var x : nat
4 --> var y : nat
x --> y

[] --> var m : map any nat => any nat
[ { key |-> 0, value |-> x } ] --> m

try {
    4 --> y
} catch {
    5 --> y
}

false --> var b : bool

[1,2,3] --> var nums : list nat

nums --[ [ any nat; 4 ] ]-> var dest : list nat

[4,5,6] --[ [ any nat; 4 ] ]-> dest

[true,false] --[ any such that id() ]-> var bs : list bool

y --[ 2 ]-> var z : nat

