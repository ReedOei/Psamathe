type token is unique consumable asset nat

type Bid is {
    sender : ! address,
    amount : any nat
}

;

1 --> var x : nat

4 --> var y : nat

x --> y

[ ! { key : any nat, value : any nat } ; ] --> var m : map any nat => any nat

